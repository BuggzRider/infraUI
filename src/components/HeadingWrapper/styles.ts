/* eslint-disable @typescript-eslint/no-unused-vars */
import { css } from "@emotion/react";

export const headerWrapperContainer = (theme: any) => css`
  width: 100%;
  height: 100%;
  label: headerWrapperContainer;
`;

export const headerWrapperHedaing = (theme: any) => css`
  font-size: 36px;
  color: #000;
  font-family: var(--CinzelFont-font);
  text-align: center;
  margin-bottom: 70px;
  text-transform: uppercase;
  label: headerWrapperContainer;
`;
